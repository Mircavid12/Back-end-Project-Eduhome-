@model List<Blogs>
@{
    ViewData["Title"] = "Blog";
}

<div class="col-lg-12 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <h4 class="card-title">Blogs</h4>
            <a asp-controller="Blog" asp-action="Create" class="btn btn-success">
                Create
            </a>
            <div class="table-responsive">
                <table class="table table-striped">
                    <thead>
                        <tr>
                            <th>
                                Blog Description
                            </th>
                            <th>
                                Settings
                            </th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (Blogs b in Model)
                        {
                            <tr>
                                <td>
                                    @b.Description
                                </td>
                                <td>
                                    <a asp-controller="Blog" asp-action="Detail" asp-route-id="@b.id" class="btn btn-secondary">
                                        <i class="mdi mdi-information"></i>
                                    </a>
                                    <a asp-controller="Blog" asp-action="Update" asp-route-id="@b.id" class="btn btn-warning">
                                        <i class="mdi mdi-table-edit"></i>
                                    </a>
                                    <a asp-controller="Blog" asp-action="Delete" asp-route-id="@b.id" class="btn btn-danger">
                                        <i class="mdi mdi-delete"></i>
                                    </a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

